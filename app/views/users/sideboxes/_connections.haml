#connections.sidebox
  %h5="Connected to #{user.connections_to_people.size} #{user.connections_to_people.size == 1 ? 'person' : 'people'}"
  = render :partial => '/users/sideboxes/user.haml', :collection => user.connections_to_people