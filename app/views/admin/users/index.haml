#content-header-container
  #content-header
    #user-profile-header

#content-main-container
  #content-main-wrapper
    #content-main-wide
      =render :partial => "/admin/navigation"
      %h1="All users"
      %table.organizations
        %thead
        %tbody
          - @users.each do |user|
            %tr
              %td=user.id
              %td=user.full_name
              %td=link_to 'view profile', person_path(user), :target => "_blank"
